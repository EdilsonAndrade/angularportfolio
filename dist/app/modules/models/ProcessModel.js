var app;
(function (app) {
    var models;
    (function (models) {
        var Process = (function () {
            function Process() {
            }
            return Process;
        }());
        models.Process = Process;
    })(models = app.models || (app.models = {}));
})(app || (app = {}));
